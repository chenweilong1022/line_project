<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.ltt.dao.CdMaterialDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.renren.modules.ltt.entity.CdMaterialEntity" id="cdMaterialMap">
        <result property="id" column="id"/>
        <result property="remark" column="remark"/>
        <result property="uploadNumber" column="upload_number"/>
        <result property="synchronizationsNumber" column="synchronizations_number"/>
        <result property="successesNumber" column="successes_number"/>
        <result property="failuresNumber" column="failures_number"/>
        <result property="deleteFlag" column="delete_flag"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="listPage" resultType="io.renren.modules.ltt.vo.CdMaterialVO">
        SELECT
            m.id,
            m.remark,
            m.material_url materialUrl,
            m.navy_url navyUrl,
            m.upload_number uploadNumber,
            m.material_number materialNumber,
            m.navy_number navyNumber,
            m.synchronizations_number synchronizationsNumber,
            m.successes_number successesNumber,
            m.failures_number failuresNumber,
            m.type,
            m.delete_flag deleteFlag,
            m.create_time createTime,
            t.group_status groupStatus,
            t.group_name groupName,
            t.room_id roomId,
            t.id groupTaskId
        FROM
            cd_material m
                LEFT JOIN cd_group_tasks t ON m.id = t.material_id
        order by m.id desc
        <where>

        </where>
    </select>


</mapper>
